services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:8000:8000"
    env_file: /etc/bulletin.env
    environment:
      TZ: America/Phoenix
    volumes:
      - build:/app/build
      - archive:/app/archive

volumes:
  build:
  archive:
